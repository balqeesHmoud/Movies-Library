CREATE TABLE movie_data (
    -- 'title' column to store the title of the entry, with a maximum length of 255 characters.
    tiltle varchar(255) ,
    -- 'overview' column to store an overview of the entry, with a maximum length of 255 characters.
    overview varchar(255) ,
     -- 'img' column to store the image path or URL, with a maximum length of 255 characters.
    img varchar (255)
   
);